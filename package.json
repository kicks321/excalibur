{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "server": "clear && cd packages/server && yarn dev"
  },
  "devDependencies": {
    "lerna": "^5.1.2"
  }
}
